import 'package:flutter/material.dart';

class MyModalOrder extends StatelessWidget {
  const MyModalOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
